{
  "name": "sfbayferry-trmnl",
  "private": true,
  "description": "Node tooling for the TRMNL plugin (compiles transform-src/transform.ts to src/transform.js, renders docs/screenshots/*.png via headless Chromium) plus the Swiftly API exploration scripts (scripts/explore-routes.mjs etc.). The plugin itself (src/*.liquid, settings.yml) is otherwise a trmnlp (Ruby) project.",
  "type": "module",
  "scripts": {
    "screenshots": "node scripts/screenshots.mjs",
    "build:transform": "tsc -p transform-src/tsconfig.json",
    "typecheck:transform": "tsc -p transform-src/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "playwright": "^1.48.0",
    "typescript": "^6.0.3"
  }
}
